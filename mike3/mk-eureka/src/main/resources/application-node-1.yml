server:
  port: 9011

eureka:
  instance:
    hostname: mk-eureka1
  client:
    register-with-eureka: true # 注册到eureka
    fetch-registry: true # 获取注册信息
    service-url:
      defaultZone: http://mk-eureka2:9012/eureka
